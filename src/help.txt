Hotdog Detector Subcommands:

hdai train <ct>
-   Train a model using images in the ./dataset folder, with `ct` models per generation.
hdai det <image>
-   Test on an image
hdai det <folder> all
-   Test on a folder of images
